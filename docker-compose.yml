services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: formsetu
      POSTGRES_USER: formsetu
      POSTGRES_PASSWORD: formsetu
    ports: ["5432:5432"]
    volumes: ["formsetu_postgres:/var/lib/postgresql/data"]
volumes:
  formsetu_postgres:
